# Local execution profile (DuckDB stand-in for Snowflake -- see docs/09-dbt-models.md
# Section 1 for why; production profile would target Snowflake per Phase 12).
# Run dbt commands from this directory with:  DBT_PROFILES_DIR=. dbt <command>
loan_delinquency_command_center:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: dbt_warehouse.duckdb
      threads: 4
